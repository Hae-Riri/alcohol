server:
  port: 9011

spring:
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    properties:
      hibernate:
        #show_sql : true
        format_sql: true
        query:
          in_clause_parameter_padding: true
    open-in-view: false